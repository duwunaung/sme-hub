<%- include('../partials/superadmin/header.ejs') %>
    <%- include('../partials/superadmin/navbar.ejs') %>


        <!-- Main Content -->
        <main class="col-md-9 col-lg-10">
            <div class="content">

                <% if (errorMessage) { %>
                    <div id="errorBox" class="alert alert-danger" role="alert">
                        <%= errorMessage %>
                    </div>
                    <% } %>
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h2>Organizations</h2>
                            <button class="btn btn-primary" data-bs-toggle="modal"
                                data-bs-target="#addOrganizationModal">
                                Add New Organization
                            </button>
                        </div>

                        <!-- Organizations Table -->
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead class="table-dark">
                                    <tr>
                                        <th>#</th>
                                        <th>Organization Name</th>
                                        <th>Status</th>
                                        <th>Expiration Date</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody id="organizationsTable">
                                    <!-- Dynamic rows go here -->
                                    <% for (let i=0; i < orgs.length; i++) { %>
                                        <tr>
                                            <td>
                                                <%= i+1 %>
                                            </td>
                                            <td>
                                                <%= orgs[i].name %>
                                            </td>
                                            <td>
                                                <%= orgs[i].status %>
                                            </td>
                                            <td>
                                                <%= new Date(orgs[i].expiredDate).toDateString() %>
                                            </td>
                                            <td>
                                                <a href="/superadmin/organizations/<%= orgs[i]._id %>"
                                                    class="btn btn-primary btn-sm">View</a>
                                                    <a href="/superadmin/organizations/update/<%= orgs[i].id %>"
                                                    class="btn btn-warning btn-sm">Edit</a>

                                                <% if (orgs[i].status=='deleted' ) { %>
                                                    <a href="/superadmin/organizations/restore/<%= orgs[i].id %>"
                                                        class="btn btn-danger btn-sm">Restore</a>
                                                    <% } else {%>
                                                        <a href="/superadmin/organizations/delete/<%= orgs[i].id %>"
                                                            class="btn btn-danger btn-sm">Delete</a>
                                                        <% } %>
                                            </td>
                                        </tr>
                                        <% } %>
                                </tbody>
                            </table>
                        </div>
            </div>
        </main>

        <%- include('../partials/superadmin/footer.ejs') %>