<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Organization</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container my-5">
    <h2 class="mb-4">Update Organization</h2>

    <!-- Organization Details Form -->
    <form id="updateOrganizationForm" class="mb-4">
      <div class="mb-3">
        <label for="orgName" class="form-label">Organization Name</label>
        <input type="text" class="form-control" id="orgName" placeholder="Enter organization name" required>
      </div>
      <div class="mb-3">
        <label for="orgAddress" class="form-label">Address</label>
        <textarea class="form-control" id="orgAddress" rows="2" placeholder="Enter organization address" required></textarea>
      </div>
      <div class="mb-3">
        <label for="orgPhone" class="form-label">Phone</label>
        <input type="tel" class="form-control" id="orgPhone" placeholder="Enter phone number" required>
      </div>
      <div class="mb-3">
        <label for="orgStatus" class="form-label">Status</label>
        <select id="orgStatus" class="form-select" required>
          <option value="active">Active</option>
          <option value="deleted">Deleted</option>
        </select>
      </div>
      <button type="submit" class="btn btn-primary w-100">Update Organization</button>
    </form>

    <!-- License Extension -->
    <div>
      <h4>License Extension</h4>
      <p>Extend the organization's license expiration date:</p>
      <div class="d-flex gap-2">
        <button class="btn btn-outline-secondary" onclick="extendLicense(5)">+5 Days</button>
        <button class="btn btn-outline-secondary" onclick="extendLicense(30)">+1 Month</button>
        <button class="btn btn-outline-secondary" onclick="extendLicense(180)">+6 Months</button>
      </div>
      <p class="mt-3">Current Expiration Date: <span id="currentExpirationDate" class="fw-bold">2024-12-31</span></p>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
