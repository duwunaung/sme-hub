<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Organizations</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            overflow-x: hidden;
        }

        #sidebar {
            min-height: 100vh;
        }

        .content {
            padding: 20px;
        }
        
        .input-error {
            box-shadow: 0 0 10px rgb(231, 76, 60, 0.8) !important;
            outline: none !important;
        }
		.input-warning {
			box-shadow: 0 0 10px #E4A11B !important;
            outline: none !important;
		}
        .searchBar {
            width: 40% !important;
        }
        .searchBtn:hover, .searchBtn:focus, .sortBtn:hover, .sortBtn:focus, .dropdown-item:hover, .useFilter {
            background-color: #0d6efd !important;
            color: #fff !important;
        }
        .dropdown-active {
            color: #0d6efd !important;
        }

        /* Tooltips */
        .icons, .usericons{
            position: relative;
        }

        .icons::before, .icons::after, .usericons::before, .usericons::after {
            --scale: 0;
            --arrow-size: 10px;

            position: absolute;
            transform: translateY(-50%) translateX(var(--translate-x, 0)) scale(var(--scale));
            top: 50%;
            transition: 150ms transform;
            transform-origin: right;
        }

        .icons::before, .usericons::before {
            --translate-x: calc(-100% - var(--arrow-size));

            content: attr(data-tooltip);
            color: #fff;
            padding: 0 .5rem .3rem .5rem;
            border-radius: .3rem;
            width: max-content;
            background-color: var(--tooltip-color);
        }

        .icons:hover::before, .icons:hover::after, .usericons:hover::before, .usericons:hover::after {
            --scale: 1;
        }

        .icons::after, .usericons::after {
            --translate-x: calc(-1 * var(--arrow-size));

            content: '';
            border: var(--arrow-size) solid transparent;
            left: 25px;
            border-left-color: var(--tooltip-color);
            transform-origin: left center;
        }

        .icons::after {
            left: 25px;
        }

        .usericons::after {
            left: 18px;
        }

    </style>
</head>

<body class="bg-light">
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <nav id="sidebar" class="col-md-3 col-lg-2 bg-dark text-white p-3">