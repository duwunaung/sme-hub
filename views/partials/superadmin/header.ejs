<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Organizations</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            overflow-x: hidden;
            --btnColor: #fff;
            --btnBgColor:  #D62828;
            --pagniColor:  black;
            
            /* <= primaryColorCode - #007bff */
        }

        #sidebar {
            min-height: 100vh;
        }

        .content {
            padding: 20px;
        }
        
        .input-error {
            box-shadow: 0 0 10px rgb(231, 76, 60, 0.8) !important;
            outline: none !important;
        }
		.input-warning {
			box-shadow: 0 0 10px #E4A11B !important;
            outline: none !important;
		}
        .searchBar {
            width: 40% !important;
        }

        /* Tooltips */
        .icons, .usericons, .saIcons {
            position: relative;
        }

        .icons::before, .icons::after, .usericons::before, .usericons::after, .saIcons::before, .saIcons::after {
            --scale: 0;
            --arrow-size: 10px;

            position: absolute;
            transform: translateY(-50%) translateX(var(--translate-x, 0)) scale(var(--scale));
            top: 50%;
            transition: 150ms transform;
            transform-origin: right;
        }

        .icons::before, .usericons::before, .saIcons::before {
            --translate-x: calc(-100% - var(--arrow-size));
			
            content: attr(data-tooltip);
            color: #fff;
            padding: 0 .5rem .3rem .5rem;
            border-radius: .3rem;
            width: max-content;
            background-color: var(--tooltip-color);
        }

        .icons:hover::before, .icons:hover::after, .usericons:hover::before, .usericons:hover::after, .saIcons:hover::before, .saIcons:hover::after {
            --scale: 1;
        }

        .icons::after, .usericons::after, .saIcons::after {
            --translate-x: calc(-1 * var(--arrow-size));

            content: '';
            border: var(--arrow-size) solid transparent;
            left: 25px;
            border-left-color: var(--tooltip-color);
            transform-origin: left center;
        }

        /* Arrow margin */
        .icons::after {
            left: 25px;
        }

        .usericons::after {
            left: 18px;
        }

        .saIcons::after {
            left: 23px;
        }

        /* Btns */
        .btnColor, .sortBtn:hover, .sortBtn:focus, .useFilter, .searchBtn:hover, .searchBtn:focus, .page {
            background-color: var(--btnBgColor) !important;
            border: 1px solid var(--btnBgColor) !important;
            color: var(--btnColor) !important;
        }
        .dropdown-item:hover {
            background-color: gainsboro;
        }
        .dropdown-active {
            color: var(--btnBgColor);
        }
        .page-link, .page-link:hover {
            color: var(--pagniColor);
        }
        .pagContent {
            color: var(--pagniColor);
        }

        /* Navbar */
        #sidebar {
            border: 1px solid rgb(120, 111, 111);
            box-shadow: 0px 4px 25px rgb(112, 106, 106);
        }

        .nav-link, .nav-link:hover {
            color: black;
            font-weight: 600;
            text-decoration: none;
        }

        .nav-link-active, .nav-link-active:hover {
            color: #fff;
            font-weight: 600;
        }

        .nav-link-active:hover {
            cursor: default;
        }

        .nav-item-active {
            background-color: var(--btnBgColor);
            border: 1px solid var(--btnBgColor);
            border-radius: 10px;
        }

        /* LOGO */
        #menuBtn, #profileBtn {
            width: 51px;
            height: 56px;
            border-radius: 8px;
            top: 25px;
            left: 39.42px;
            padding: 10px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            border: none;
            cursor: pointer;
            z-index: 1031;
        }

        #menuBtn {
            background-color: var(--btnBgColor);
            cursor: default;
        }

        #mobile {
            display: none;
        }

        #pcProfile {
            display: flex;
        }

        /* Tablet responsive */
        @media screen and (max-width: 1024px) {
            #sidebar {
                display: none;
            }
            #pcProfile {
                display: none;
            }
            #mobile {
                display: block;
            }
            .offcanvas {
                --bs-offcanvas-width: 250px;
            }
        }

        /* Phone responsive */
        @media screen and (max-width: 767px) {
            #sidebar {
                display: none;
            }
            #pcProfile {
                display: none;
            }
            #mobile {
                display: block;
            }
            .offcanvas {
                --bs-offcanvas-width: 250px;
            }
        }

    </style>
</head>

<body class="bg-light">
    <div class="container-fluid">
        <div class="row">